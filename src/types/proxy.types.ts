export interface WebshareProxy {
  username: string;
  password: string;
  proxy_address: string;
  port: number;
}

export interface Proxy {
  username: string;
  password: string;
  address: string;
  port: number;
}
